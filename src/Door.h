#include <Arduino.h>
#include "Define.h"

extern bool auto_refill; //True -> automode, False -> manualmode
extern bool open_door; //True -> open, False -> closed

void Door(void *param) {
    while(1) {
        
    }
}